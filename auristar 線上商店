<!DOCTYPE html>
<html lang="zh-Hant">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Auristar 遊戲商城</title>
  <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+TC:wght@400;700&display=swap" rel="stylesheet">
  <style>
    body {
      margin: 0;
      font-family: 'Noto Sans TC', sans-serif;
      background: #0d0d0d;
      color: #fff;
      overflow-x: hidden;
    }
    header {
      background: linear-gradient(to right, #111, #222);
      text-align: center;
      padding: 40px 20px;
      position: relative;
      z-index: 1;
    }
    header img {
      max-width: 180px;
      height: auto;
    }
    h1 {
      color: gold;
      font-size: 2.5em;
      margin: 20px 0 10px;
    }
    p.subtitle {
      font-size: 1.2em;
      color: #ccc;
      margin-bottom: 40px;
    }
    section.intro {
      max-width: 800px;
      margin: auto;
      padding: 20px;
      text-align: center;
      position: relative;
      z-index: 1;
    }
    section.intro h2 {
      color: gold;
      font-size: 1.8em;
    }
    ul.features {
      list-style: none;
      padding: 0;
      margin: 20px 0;
    }
    ul.features li {
      font-size: 1.1em;
      margin: 10px 0;
    }
    .cta-button {
      display: inline-block;
      background: gold;
      color: #000;
      font-size: 1.3em;
      font-weight: bold;
      padding: 15px 30px;
      border-radius: 8px;
      text-decoration: none;
      margin-top: 30px;
      transition: background 0.3s;
    }
    .cta-button:hover {
      background: #ffce00;
    }
    footer {
      text-align: center;
      font-size: 0.9em;
      color: #888;
      padding: 30px 20px;
      position: relative;
      z-index: 1;
    }
    #starfield {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      z-index: 0;
    }
    audio {
      display: block;
      margin: 20px auto;
      z-index: 2;
      position: relative;
    }
  </style>
</head>
<body>
  <canvas id="starfield"></canvas>
  <header>
    <img src="./7045dc2f-de6b-4e3b-97e8-2e173c76e31b.png" alt="Auristar 標誌">
    <h1>✩ Auristar 遊戲商城</h1>
    <p class="subtitle">Auristar — 星耀遊戲商城，由你主定</p>
  </header>

  <section class="intro">
    <p>Auristar 是一個專注於 <strong>遊戲代償、代購與交易服務</strong> 的全方位商城平臺，結合閃耀的金色星光與專業團隊，為每一位玩家帶來安心、快速又潮流的遊戲體驗。</p>

    <h2>為什麼選擇 Auristar？</h2>
    <ul class="features">
      <li>🛡️ 安全誠信可靠</li>
      <li>📞 快速回應客服</li>
      <li>💎 多年遊戲圈經驗</li>
      <li>🔥 不定期限時活動 & 優惠回饋</li>
    </ul>

    <a class="cta-button" href="https://goldenstar8787.github.io/auristar-on-line--store/" target="_blank">選我購買</a>
    <audio controls loop>
     <source src="https://cdn.pixabay.com/audio/2023/03/30/audio_84ac8c8d91.mp3" type="audio/mp3">
   你的瀏覽器不支援播放音樂。
    </audio>
  </section>

  <footer>
    &copy; 2025 Auristar. All rights reserved.
  </footer>

  <script>
    // 星塵背景動畫
    const canvas = document.getElementById('starfield');
    const ctx = canvas.getContext('2d');
    canvas.width = window.innerWidth;
    canvas.height = window.innerHeight;

    const stars = Array.from({ length: 150 }, () => ({
      x: Math.random() * canvas.width,
      y: Math.random() * canvas.height,
      r: Math.random() * 1.5 + 0.5,
      d: Math.random() * 0.5 + 0.2
    }));

    function drawStars() {
      ctx.clearRect(0, 0, canvas.width, canvas.height);
      ctx.fillStyle = '#ffffff';
      stars.forEach(s => {
        ctx.beginPath();
        ctx.arc(s.x, s.y, s.r, 0, Math.PI * 2);
        ctx.fill();
        s.y += s.d;
        if (s.y > canvas.height) {
          s.y = 0;
          s.x = Math.random() * canvas.width;
        }
      });
      requestAnimationFrame(drawStars);
    }
    drawStars();

    window.addEventListener('resize', () => {
      canvas.width = window.innerWidth;
      canvas.height = window.innerHeight;
    });

    // 防止開發者工具（F12、Ctrl+Shift+I / J、Ctrl+U）
    document.addEventListener('keydown', function (e) {
      if (
        e.key === 'F12' ||
        (e.ctrlKey && e.shiftKey && (e.key === 'I' || e.key === 'J')) ||
        (e.ctrlKey && e.key === 'U')
      ) {
        alert('此操作已被禁止！');
        e.preventDefault();
      }
    });

    // 禁止右鍵
    document.addEventListener('contextmenu', function (e) {
      alert('禁止使用右鍵');
      e.preventDefault();
    });
  </script>
</body>
</html>
